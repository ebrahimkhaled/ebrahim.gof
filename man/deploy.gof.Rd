% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cdef_gof.R
\name{deploy.gof}
\alias{deploy.gof}
\title{Deployable learned-ensemble GOF test via parametric bootstrap}
\usage{
deploy.gof(object, meta, B = 99, feature_fn = gof.features)
}
\arguments{
\item{object}{A fitted binary logistic \code{\link[stats]{glm}}.}

\item{meta}{A pre-trained scorer: either a function \code{f(features)} returning
a scalar misfit score, or an object with a \code{predict} method consuming a
one-row feature \code{matrix}.}

\item{B}{Number of parametric-bootstrap resamples (default 99).}

\item{feature_fn}{Function mapping a fitted glm to its feature vector (default
\code{\link{gof.features}}).}
}
\value{
A one-row \code{data.frame} with the score, \code{B}, and the
  bootstrap \code{p_value}.
}
\description{
Turns a pre-trained ensemble \code{meta} into a deployable goodness-of-fit
test for \emph{any} fitted model: it scores the model, then calibrates the
p-value by a per-dataset parametric bootstrap from the fitted model (so no
knowledge of the truth or the data-generating design is required). Validity
comes from the bootstrap, independent of how \code{meta} was trained.
}
\seealso{
\code{\link{gof.features}}, \code{\link{cdef.gof}}.
}
